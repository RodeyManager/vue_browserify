;(function(){
    var App = {
        // 接口host
        ServerHost: '', //(xxx)
        method: 'POST',
        sendTime: 60,
        debug: true
    };

    this['App'] = App;
    return App;

}).call(this || window);
